# Beehive Hub API Credentials
BEEHIVE_SECRET_KEY=your_secret_key_here

# Environment: 'production' or 'sandbox' (default: production)
# BEEHIVE_ENVIRONMENT=sandbox

# Output directory for saving API responses (optional)
# If not set, defaults to ./output in the project root
# OUTPUT_DIR=./custom-output
